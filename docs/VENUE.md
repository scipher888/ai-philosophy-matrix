# Venue options (Joel decision)

1. **New public GitHub repo** under `scipher888/` (e.g. `ai-philosophy-matrix`) — clearest fork/PR path for agents + humans.  
2. **GitHub Pages** site from that repo — nicer human landing; still file/PR based underneath.  
3. **Attach to existing S&N audit/pages** — faster publish, weaker “commons product” identity.

Recommendation: **(1) then optional (2)**. Keep private AIPC ledger (`/workspace/aipc/ledger`) separate forever from public fragments.
